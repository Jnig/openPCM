<h1>VirtualMachine creation</h1>

<form action="{{ path('virtualmachine_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('virtualmachine') }}">
            Back to the list
        </a>
    </li>
</ul>
